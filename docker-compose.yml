services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./policies:/app/policies
      - ./criteria:/app/criteria
      - ./exceptions:/app/exceptions
